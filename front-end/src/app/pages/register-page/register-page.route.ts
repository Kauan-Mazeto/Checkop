import { Route } from '@angular/router';
import { RegisterPageComponent } from './register-page.component';

export const registerPageRoute: Route = {
  path: 'register',
  component: RegisterPageComponent,
};
