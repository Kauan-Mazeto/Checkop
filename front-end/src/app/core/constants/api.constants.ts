// No ambiente de desenvolvimento, o Angular roda em localhost:4200 e usa o proxy
// definido em proxy.conf.json para encaminhar /api para o backend em 8080.
// Isso evita o problema de CORS sem mexer no servidor.
export const API_BASE_URL = '/api';
